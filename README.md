# Node.js CRUD API
An API built using Node.js, Express, Mongoose, Body-Parser, Dotenv and Cors that can perform all CRUD operations with Mongo DB as the database. To start the script, type in *npm start*
